# Weather App

A simple Python weather application that generates a weather report based on user inputs such as city name, temperature, and humidity.

## Features
- Takes city name as input
- Displays temperature and humidity
- Predicts weather condition using Python if-else statements
